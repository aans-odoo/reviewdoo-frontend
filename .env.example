# ===========================================
# Reviewdoo — Frontend Config
# ===========================================

# Backend API URL used during development (Vite proxy target)
# In production, the frontend is served behind a reverse proxy that routes /api to the backend
VITE_API_URL="http://localhost:3000"

# Frontend dev server port (default: 5173)
VITE_PORT=5173
